<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tsy.mapper.AuthMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user_base (username, password, email, role, status, create_time, phone)
        VALUES (#{username},#{password},#{email},#{role},#{status},#{createTime},#{phome})
    </insert>
    <update id="updateBaseFields">
        UPDATE user_base
        SET username = #{username},
            phone = #{phone},
            email = #{email},
            age = #{age},
            status = #{status}
        WHERE id = #{id}
    </update>

</mapper>
